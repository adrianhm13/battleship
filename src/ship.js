class Ship {
    constructor(length){
        this.length = length;
        this.hits = [];
    }

    isHit() {
        
    }

    isSunk() {

    }
}

// eslint-disable-next-line import/prefer-default-export

module.exports = Ship;